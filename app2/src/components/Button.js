import React from 'react';

const Button = () => (
  <div>
    <p>
      More react components from <strong>App2</strong>
    </p>
    <button>App 2 Button</button>
  </div>
);

export default Button;
